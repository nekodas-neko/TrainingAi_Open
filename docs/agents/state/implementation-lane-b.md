# 🚧 Implementation Agent (B) — baton

> **Successor sessions are titled `🚧 Implementation Agent (B) 🟢`** — exactly. A renamed successor is a lost thread.

**Updated:** 2026-09-17 · **By:** the thirty-fifth Lane B run · **Next ID:** `LB-119`

> **A mistyped ID here silently advances the lane's numbering.** Allocate with `grep -rhoE '\bLB-[0-9]+\b' docs/ | sort -t- -k2 -n | tail -1`, and check the max in the **journal** too — a shipped entry leaves the queue. The pointer above is a floor, not an allocation.

## Now

**Thirty-six PRs, v1.456.0→v1.457.11.** LB-107 · LB-108 · LB-106 · PS-35a · the journal fold (40 entries) · BF-161 · BF-100's one-tap experiment surfaced · BF-162 (the card said "load 85kg" onto a Hanging Leg Raise) · LA-109 (a tab flip left the previous tab's route tree on the history entry) · BF-163 (the chip's tooltip claimed reps it does not read) · RV-38 (the "Limited data" badge exempted the case it is most true of) · OR-116 (one metric name over two different heart rates) · TN-13 closed on an owner decision · BF-166 (back ignored the overlay stack the app already had) · BF-74 round two (removing a meal photo asks first) · **LA-108** (a declined weigh-in can be claimed back) · #1239 (retracting LB-110) · **BF-169 · BF-168 · BF-167** shipped as one batch — the COMPLETED stamp waited on a fetch it has nothing to do with, "Leave workout?" fired on the session-select *tab*, and the intensity toggle read a phase flag an illness deload never sets · **BF-170** (a diary meal showed its macros nowhere) · **BF-5 PR 2b** — the week in review is a page, which CLOSES that entry · **LB-111** filed (docs-only): OR-118's engine half · **LB-113** (Health Connect synced everyone into Brisbane) · **LB-114** filed (RV-38's spec is red 07:00-08:00 Brisbane DAILY; Lane A's) · **BF-172** (the explain ring called a fit score readiness) · **LB-116** (the check-in threw away which sore ticks it suggested) · **BF-175** (two calorie budgets two taps apart, plus a second surface its own sweep could not see) · **LB-118** filed (Lane A: explain `signals` has no sore provenance, which parks LB-117). **`check:rules` is `Ran 75 of 75` now, not 74** — the count moves, which is why the rule says quote it rather than "pass".

**BF-110 still waits on the owner:** one blank resume in normal use, then read `bf110 resume recheck%` — **`stuck` → native, `resized` → render timing**. No fix before that row.

**A 4-hourly silent Routine polls this lane** (`trig_01WcuYTidPtngLFZFD7yKnoL`): syncs `main`, clears any open PR, runs `next-item.js`, **says nothing when READY is 0**.

## Next

1. **RE-SCAN BEFORE CONCLUDING ANYTHING — this has now fired SIX times**, most recently when BF-166 was filed by BugFix and went straight to the top of READY while I was mid-PR on OR-116. READY went 6→9 on Lane A's BF-155; BF-161 became startable minutes after I reported the lane empty; the queue went **319 → 330** overnight; and the Orchestrator's OR-116 sweep changed the lane while this baton was mid-rewrite. **A "blocked" or "empty" finding has a shelf life of one merge.**
2. **⛔ BF-165: MY HARNESS REPRODUCTION WAS RETRACTED (2026-09-15). It does NOT reproduce; there is NO second dead button; the `/activity`-prefix narrowing is void.** The cause was **`next dev` compiling the route on demand** — a client push issues an RSC fetch that hangs until compilation finishes (unresolved after 8 s while sibling `/api/*` calls returned 200), with no throw, no 4xx/5xx, no console error, and no URL change because Next commits on response. **Byte-for-byte a dead tap.** Warm the destination first with a direct `goto` and the tap works (200). **THE RULE: warm the destination before measuring ANY client-side push** — without it "the navigation did not happen" carries no information, and no fixed wait is safe because compile time scales with the tree. BF-165 is back to **device-gated, cause unknown, three candidates open**; only the source-path elimination table (from reading) survives, and the sheet-`history.back()` theory is back to UNPROVEN.
3. **ASKING BEAT BUILDING on TN-13.** Its fix was one line and I was about to pick a presentation; put to the owner as four drawn options, the answer was **leave it** — the HR chip would be the only cell with a second line. A one-line fix is not thereby a cheap decision. **Two owner questions remain open**, both shipped-around rather than blocked on: RV-38's `—`-versus-badge posture, and OR-116's three-surfaces-one-number context (BF-163's load-vs-reps is Lane A's once ruled on). **And one possible real defect, recorded not fixed:** `/health/heart-rate` passes `hrMin` into `HrFactorsCard` as `restingHr` — an intraday minimum standing in for a resting rate (50 against 60). Establish whether it is a deliberate proxy BEFORE changing it. **BF-167 left one too:** the prescription's rationale prose still names the pre-deload band against deloaded rows — Lane A's, filed on the entry.
4. **THE LANE IS NOT DEVICE-BLOCKED — BF-165 above is live buildable work.** BF-100 is the blocked one and its harness route is closed off. **Do not re-run the synthetic-`touchstart` probe** — it was driven 2026-09-15, both arms restored (840), and that refutes the PROBE not the hypothesis: the element picked need not be the hook's `ref` and the dispatch may miss `RESTORE_WINDOW_MS`. Instrument both before reading anything into a re-run; the one-tap device test (come back with a UI back control, not the gesture) is still what settles it. Everything else is KEEP. **BF-61's fast-tap check is the keystone of clearing it:** it releases BF-94 (owner-approved, otherwise ready) and the same S25 sitting clears BF-100, BF-74 round two, LA-109, BF-166 and most of the other KEEPs. **Re-run `next-item.js` anyway** — that finding has expired six times.
5. **READY running low is not "no work" — READ THE KEEPS WHOLE, and this just paid out.** **LA-108 printed under KEEP as "not new work" for a day while its residue WAS the work** — *"the list, and only the list"*, a buildable UI task with a lane, a file and three notes. Shipped 2026-09-15. `next-item.js`'s KEEP header (*"only the stated residue is owed"*) is a category, not a judgement: a residue can be a device check OR a build. **It paid out AGAIN on 2026-09-16, bigger:** with READY down to two device-gated entries, **BF-5's `Keep:` was `PR 2b, the surface`** — a whole feature with a written plan, printing as "not new work". **When READY is thin, read every KEEP's residue for the word that means BUILD.** Also read PARKED (the console truncates); TN-3b sat there three days while READY was 0. **⛔ AND DO NOT REPEAT MY LB-110 MISTAKE (removed in #1238; corrected in #1239):** I saw TN-3b's `Keep:` waiting on `LB-102`, found no such heading, verified `/api/body-battery` takes no date, and filed the work to build it. **Both facts were true and the conclusion was wrong** — `stress-day/route.ts` had served stored buckets by date since 2026-09-13. Absence from a queue that removes completed entries is the signature of FINISHED. **Verifying the inputs is not verifying the conclusion: ask whether the capability exists ANYWHERE before filing work to build it.** **BF-51 ① is the trap** — built, deliberately unshipped; reproduce on the S25 first, never loosen `meal-photo-picker.spec.ts`.

## Blocked

- **Owner decisions:** BF-126, Q-551, the macro/budget anchor, LB-61's switch colour, PS-35's PWA landing.
- **LB-106 is open with its fix UNCLAIMED.** Its stated cause was wrong: the log says `page.goto: net::ERR_ABORTED` at the **relaunch**, so the poll it blamed is never reached. **If it aborts again on `fresh.goto`, the relaunch shape was not it either** — drop the SW block and suspect the runner.
- **BF-100 is UNBLOCKED as of LA-109 shipping**, and its one-tap test has still never been run: `use-scroll-restoration.ts` cancels its pending restore on **`touchstart`** with no re-arm; the S25 back gesture is a touch, `page.goBack()` is not. **The test: come back with a UI back control, not the gesture** — every device pass used the gesture because the entry's own verification step says to. **Do not build the `touchmove` fix on the hypothesis.**
- **BF-49 needs a device repro and nothing else.** Its LA-109 link is refuted (below); do not re-derive it.
- **~50 VERIFY entries owe a look** (BF-136/LB-99 sharpest; BF-157's chip-vs-bar is native). **⚠ OR-108's picture will not show on the S25 until `LA-36` lands** — the local-store reads omit `image_data_uri`; web is fine.
- **⚠ BF-94 is parked on `Needs: BF-61`. ⚠ BF-84 reads startable and is not. PS-4 is UNCLASSIFIED by design; LB-94 the owner deferred.** Do not classify either.
- **⚠ Q-254 is device-free Lane B work parked behind `Needs: Q-297`**, itself `Gate: owner`; its premise is stale (says one spec exists, there are **84**). Do not unpark.

## Claimed paths

None held. #265 and #608 are abandoned Lane B PRs whose work is on `main`; closing one needs the owner.

## Do not re-litigate

- **`packages/shared/**`, `app/api/**`, `lib/data/**`, `lib/sqlite/**`, `lib/local-store/**`, `lib/cache-groups.ts`, `lib/coach/**` are Lane A** — the **path**, not the nature of the edit. `scripts/**` is the Orchestrator's, bar a shrink-only baseline the check demands. **Clearing a completed entry is the Orchestrator's sweep too — file it, do not do it.**
- **A Lane B half needing a Lane A argument is TWO entries.** `workout-screen.tsx` is shrink-only at 1833 lines. **`saved-meals-sheet.tsx` is 791 against a HARD 800** — a new feature there goes in an extracted child.
- **E2E is ADVISORY** — but wait on it for app-code or new-spec PRs. **Batons are shrink-only**; cut narrative before adding.

## The lessons that cost real time

1. **RUN THE SPEC AGAINST THE UNFIXED FILE. Every time. It is two minutes and it is the only thing that distinguishes a fix from a green test.** LA-109 settled two entries in one run: its own test went red without the fix (so the fix is load-bearing) and its BF-49 cross-check **passed** without it (so that test was vacuous and the entries are not linked). Both halves were invisible from a green run. BF-146's spec also passed with its fix reverted. **A vacuous test nobody has labelled is worse than no test, because it answers.** **LB-116 is the cleanest case yet and the answer was to DELETE:** an e2e reading `suggested_sore_muscles` back passed against unfixed `main`, because `saveMoodLog` DERIVES the list when none is sent — the column is non-null either way. **When a server fallback produces the same observable as the fix, no end-to-end assertion can separate them**; the unit test (a real `MoodFieldsSchema.parse`) carried the proof instead. **Harness traps that all read as "the sheet did not open":** the morning check-in is a Radix modal that `aria-hidden`s everything behind it, the mood sheet AUTO-opens with no mood row, and the muscle pills are `role="checkbox"` not `button`. **⚠ AND RUN THE CONTROL FOR THE HARNESS, not just for the fix:** *does the happy path work here at all.* Three wrong BF-165 conclusions, one of them a fabricated defect, came from never asking it — on a dev server that compiles on demand, "it didn't work" is the default answer for anything not yet visited. **And a comment can be vacuous the same way:** BF-74's component argued in writing against a confirm because *"re-picking is already one tap"* — `getPhoto` is called with no `saveToGallery`, so for a camera capture there is nothing to re-pick. **Check a comment that is making a factual claim.** **⚠ AND YOUR CHANGE CAN MAKE ANOTHER AGENT'S TEST VACUOUS WITHOUT MAKING IT RED:** BF-168's dismissal effects added an earlier `setConfirmLeaveOpen` to the top of `mobile-auth-handler.tsx`, so BF-166's ordering check — `indexOf('setConfirmLeaveOpen') < indexOf('hasOpenSurface()')` — went on passing while measuring my effect instead of its listener guard. It would have stayed green over a file where the guards had moved below the overlay check. **A green sibling suite is not proof you left it alone; re-read any assertion that anchors on a string you just added.** Tightened to `setConfirmLeaveOpen(true)` — the raise, which only the listener does. **⚠ AND A SPEC FILE CAN COVER A COMPONENT WITHOUT BEING ABLE TO FAIL ON ITS BUG:** `diary-nested-meal.spec.ts` had four tests over `DiaryMealGroup` and asserts `P 24g` — exactly BF-170's missing figure — but taps the group OPEN first, so it passed throughout. **Before trusting a spec file as coverage, find the line that would go red.** The fifth case asserts before any tap and fails unfixed with `Expected: 1, Received: 0`.
2. **A FIELD'S JOB DONE IN PROSE IS THE MOST EXPENSIVE BUG IN THIS QUEUE — five instances.** `Reference:` filed LA-102/TN-28 as read-only (**the tell is a printed reason that is a bare LINK**). `Verify:` filed BF-100 as awaiting a look it had already FAILED twice. A **shipped entry's body** (RV-36) held BF-100's failure plus an unfiled owner request → LB-107. BF-94 printed READY while its text forbade shipping.
3. **An entry's numbers, gate and CAUSE are prose until something checks them — FOURTEEN have now been wrong.** LA-109's *"BF-49 is very likely the same defect"* was refuted by one run. **BF-166 is the worst kind: it said *"no overlay registry exists"* and one did** (`sheet-back-stack.ts` via `BackDismiss`, in BOTH primitives) — building what it asked for would have left two stacks disagreeing about what is open, ADDING a defect. **A negative claim in an entry is a grep someone ran once; re-run it with the names the repo actually uses** (`module-map.md` now carries that row, with the false-negative greps named). **⛔ AND ITS *RECOMMENDED FIX* IS PROSE TOO — two of the three entries in the 2026-09-16 batch recommended something that would have broken something else.** BF-168's proposed `pathname`-keyed dismissal would not have fired for the case it was filed on (`/workout?session=x` → `/workout` is the SAME path). BF-167's proposed *replacement* of `prescription.deload` turns BF-8's own e2e guard red — `deload-visible.spec.ts` seeds `deload: true` with an empty `exercises` array — so it shipped as a union instead. Both were caught by **reading the thing the fix would touch before implementing it**, not by a test run. **Cost: ten minutes. Cost of not: a red guard you then 'fix' by weakening it.** **⛔ A PLAN IS PROSE TOO, and BF-5's was wrong in the same shape:** it told PR 2b to keep a query param *"the cheaper option that keeps the test as-is"*, but `/api/weekly-digest` computes the recap week itself and takes none — the param would have been a control the route cannot honour, which is the exact failure that test catches. **Read the thing the plan tells you to build AGAINST, not just the plan.** **⛔ AND ITS CLAIM ABOUT WHAT DATA EXISTS IS PROSE TOO — third of this shape in two days (OR-118, 2026-09-16):** it said *"no derivation change: every number it renders already exists"*; the GROUPING did (`movementPattern`, with zero callers) and the NUMBERS did not — no route serves sets by muscle over any window but the current one, and the tonnage route is a different unit that would have overstated legs and hidden the very deficit the card is for. **One grep for the helper's callers made the claim checkable.** **⛔ SO IS ITS LIST OF CALL SITES — LB-113, the same day:** it said *"two call sites in one component"* and the component had ONE; the other was INSIDE `syncHealthConnect`, where `tz` was in scope and dropped, so fixing the component alone would have passed the entry's own pass test while enrichment kept bucketing in Brisbane. It also assumed the provider had the session; it is a bare client component (the timezone came from `useUserTimezone()`). **Grep both names yourself before trusting "small and local".** **⛔ AND ITS SWEEP CAN BE THE WRONG SWEEP — BF-175:** it asked for a grep of every `'nutrition-targets'` CACHE READ and named the four, correctly. But a budget also travels as a **prop**, and `EndOfDayReview` → `DaySummaryCard` printed the same rest-day floor as one day's denominator (with a `?? 2000` fallback under it) while reading no cache key at all. **A key-grep cannot see a value passed down a render tree — follow the VARIABLE as well as the key.** The entry's explicit *do-not-fix* (`WeeklyNutritionChart`) was right and was left alone. **AND ITS "DO NOT TOUCH X" CAN BE OVER-BROAD — BF-172:** it said change the band at the call site, *not* inside `ScoreRing`, to protect `scoreBand`'s ~15 callers. `ScoreRing` turned out to be session-explain's OWN component with one caller — a bare grep for it hits an unrelated `ScoreRingStyle` home pref, which is what makes it look shared. **Its other option ("no band word") was unusable:** the ring is band-coloured, so the word is what keeps the band off colour-only. **⏰ AND AN E2E THAT FAILS ON A CLOCK READS AS A FLAKE — LB-114, found 2026-09-16 on an unrelated PR:** `rv38-body-battery-no-data-badge` is red **07:00–08:00 Brisbane every day on every branch**, because a zero-data account's waking window is under `MIN_WAKING_MINUTES_TO_JUDGE` (60) in that hour and `sufficient` short-circuits TRUE at `sampleCount: 0`. **The control that settles ownership is reverting YOUR files and re-running** — it still failed, so it was main's. Never loosen the spec: there the test was right and the payload was wrong. Filed the exposure as LB-111 (Lane A: `app/api/**`, both halves → engine first) and parked OR-118 on it.
4. **WRITE DOWN WHAT WOULD DISPROVE YOUR FIX.** A 2026-08-30 note — *"if the abort returns, the SW was not it"* — settled LB-106 a fortnight later with **no new run**.
5. **`total_count: 0` HAS TWO CAUSES AND I CONFLATED THEM.** A stale base is one; **`mergeable_state: dirty`** is the other — GitHub could not build a merge ref, so it created **no run at all**, and `actions_list` was telling the truth. **The reliable green check is attempting the merge**, which validates against real branch protection and refuses a genuinely pending check; #1197's endpoint read two jobs `in_progress` that had already passed.
6. **A SWEEP FOR CALLERS MUST COVER ASSERTIONS, NOT JUST NAVIGATIONS.** PS-35a grepped `goto`/`href`/`push` and missed `toHaveURL(/\/workout-select/)`.
7. **A PROBE MUST CREATE THE STATE IT NEEDS.** BF-157 cost two runs. Insert by NAME, upsert, restore the referencing table before deleting. **A CI-only e2e failure looks like a missing element; the cause was in the POSTGRES SERVICE-CONTAINER LOG.** And never run `pnpm build` and vitest against the one local Postgres at once — a "failure" that will not reproduce serially is contention.

## Gotchas worth carrying

- **A leftover `next-server` from a killed run holds port 3100 without serving it, and `ss -ltn` reported nothing either way.** Every request hangs, so two tests time out at 180 s each and the run looks like a slow compile. **Check `curl -s -o /dev/null -w '%{http_code}' localhost:3100` and `ps -o etime -p <pid>` — an elapsed time older than your run is the tell.** Kill the tree before starting. **Piping the run to `tail` buffers it to EOF** — redirect to a file to watch progress.
- **RUN `pnpm vitest run` WHOLE before pushing, not just the files you touched.** BF-74 pushed red: a source-slice guard in `nutrition-uplift-guards` and three `backlog-verify-field` cases keyed on the entry's own wording. **Both were RIGHT** — the refactor moved `Undo` out of the sliced region, and a shipped round two owes `Verify: device` + `Keep:` again rather than reading as failed-on-device. A repo this rule-tested has tests aimed at your prose and at regions of your file. - **RUN `npx tsc --noEmit -p tsconfig.tests.json` BEFORE EVERY PUSH — the plain `npx tsc --noEmit` TYPECHECKS NOTHING UNDER `__tests__`.** This baton already said CI's `Build` catches it and BF-5 still pushed red on it (2026-09-16): a `/s` regex flag (needs es2018) and a `?: never` union that does not narrow on a truthiness check. **Knowing the gotcha is not running the command** — it is one line, `node scripts/check-test-typecheck.js` runs the same thing with the baseline. `Build` also catches `react-hooks/rules-of-hooks`. `e2e/` **is** covered. **After deleting a route, `tsc` fails on `.next/types/validator.ts` until a clean rebuild — that is the GENERATED file, not your source.**
- **The doc-size baseline is the rebase tax, and the check FAILS ON SLACK** — it must equal `wc -l` + 1 exactly. Recompute, never splice. `doc-size-baseline-history.md` is APPEND-ONLY, keep both with main's first; on the backlog a conflict is usually two *deletions* — keep neither, unless both sides EDITED different entries, then keep both.
- **`refusing to merge unrelated histories` is the shallow-clone graft** — `git fetch origin --deepen=150`. **Rebuild `package.json` / `changelog.ts` from `git show origin/main:…`, never splice.** **After ANY merge run `git status --short | grep '^UU'`** — the merge output's tail hid a conflict once.
- **`usePathname()` reads from Next's route tree**, so it is useless for any bug whose symptom IS a stale tree. `window.location` is the only honest reader there. **And `page.goto()` to a sub-route rebuilds history from scratch** — a spec about back must reach the screen by tapping the real affordance.
- **`locator.click()` does nothing on Nutrition, and times out on `/health`** — something animates beneath and the stability check never settles (63 retries). Use `.evaluate(el => el.click())` with a `waitForFunction` guard, or copy an opener that already works.
- **`scrollIntoViewIfNeeded()` lands a tap on the Workout tab** — use `scrollIntoView({ block: 'center', inline: 'nearest' })`. **`getByText` resolves to the INNERMOST match** and matches a tab label as readily as a heading — scope to the element.
- **The Morning Check-in is a MODAL, and Radix marks `<main>` `aria-hidden` behind it** — every role query returns nothing and the failure reads "element not found", not "a modal is in the way". Call `suppressMorningCheckin(page)` **before** `page.goto`.
- **`seed.sql` records NOTHING for today** — a spec asserting a day's content is red here, green on CI. **Judge a colour change by sampling pixels** (`OffscreenCanvas` + `getImageData`), a layout complaint by `getBoundingClientRect()` on every child.
- **The admin screens need `is_admin` AND a re-minted JWT.** Re-run `--project=setup`; restore both. **Prove a destructive path from the DATABASE, not `page.route` interception.**
