# 🚧 Implementation Agent (B) — baton

> **Successor sessions are titled `🚧 Implementation Agent (B) 🟢`** — exactly. A renamed successor is a lost thread.

**Updated:** 2026-09-17 · **By:** the thirty-fifth Lane B run · **Next ID:** `LB-120`

> **A mistyped ID here silently advances the lane's numbering.** Allocate with `grep -rhoE '\bLB-[0-9]+\b' docs/ | sort -t- -k2 -n | tail -1`, and check the max in the **journal** too — a shipped entry leaves the queue. The pointer above is a floor, not an allocation.

## Now

**Forty PRs, v1.456.0→v1.457.11.** The run before today is in the journal; today was **#1280** BF-175 (two calorie budgets two taps apart — the sheet read `nutrition_targets.calories`, the rest-day floor, and coloured its bar against it) · **#1281** BF-165's root cause · **#1282** entry fields · **#1283** LB-116 cut to its residue · **#1284** BF-100's spec can tell cancelled from imprecise. Filed: **LB-118** (Lane A, explain `signals` lacks sore provenance, parks LB-117), **LB-119** (O, chromium SIGSEGVs mid-suite in CI). Corrected: **LB-114**, mine, below. **`check:rules` is `Ran 75 of 75`** — the count moves, so quote it rather than "pass".

**⚠ THE LANE IS DEVICE-BLOCKED FOR CODE WORK, and that is a finding rather than an excuse.** READY is 1 — BF-100, whose harness **restores 840 correctly** and whose S25 does not, so nothing in the sandbox advances it. It is deliberately NOT gated: its entry argues that filing it as a pending check is what hid it for a day, and that argument is right. PARKED holds 44, all on `Needs:`/`Gate:`/owner. The KEEP residues were read whole — they are device checks, not builds. **Owed to the owner:** BF-175's S25 bar colour · BF-165's fix (design + device) · the `back-gesture-sitting` batch (BF-100, BF-166, LB-107, LA-109 — four entries, one sitting).

**A 4-hourly silent Routine polls this lane** (`trig_01WcuYTidPtngLFZFD7yKnoL`): syncs `main`, clears any open PR, runs `next-item.js`, **says nothing when READY is 0**.

## Next

1. **RE-SCAN BEFORE CONCLUDING ANYTHING — this has fired SIX times.** READY went 6→9 on Lane A's BF-155; BF-161 became startable minutes after I called the lane empty; the queue went **319 → 330** overnight. **A "blocked" or "empty" finding has a shelf life of one merge.**
2. **BF-165: ROOT CAUSE FOUND (#1281), FIX STILL OPEN.** `closeSurface` pops the entry the sheet pushed, and `selectType` runs that close immediately before `router.push` — so the pop lands on the navigation. **⛔ The obvious fix was BUILT and DOES NOT WORK:** waiting for `pendingSelfPops` to drain cannot help, because the close is **415 ms late** — it runs in an effect cleanup needing a React commit, and `startViewTransition` holds that commit, so the navigation delays the close past itself. A real fix must stop the entry being popped once a navigation supersedes it, keyed on **surface identity** (a module flag is the BF-34 known-bad). Reproduce with BOTH: warm the destination, AND land the tap (below).
3. **ASKING BEAT BUILDING on TN-13.** Its fix was one line; put to the owner as four drawn options, the answer was **leave it**. A one-line fix is not thereby a cheap decision. **Two owner questions remain open**, shipped around rather than blocked on: RV-38's `—`-versus-badge posture, and OR-116's three-surfaces-one-number. **One possible defect, recorded not fixed:** `/health/heart-rate` passes `hrMin` into `HrFactorsCard` as `restingHr` (50 against 60) — establish whether it is a deliberate proxy BEFORE changing it.
4. **READY running low is not "no work" — READ THE KEEPS AND THE ENTRIES WHOLE.** LA-108 printed under KEEP as "not new work" while its residue WAS the work. BF-5's `Keep:` was `PR 2b, the surface` — a whole feature. **It paid out a third time today from INSIDE the top READY item:** BF-100 is device-blocked, but its entry carried a second finding — an exact-offset assertion that could not tell *cancelled* from *imprecise* — which was buildable here and shipped as #1284. **When READY is thin, read every residue for the word that means BUILD.** Also read PARKED (the console truncates it); TN-3b sat there three days while READY was 0.
5. **⛔ DO NOT REPEAT MY LB-110 MISTAKE (removed in #1238, corrected in #1239):** I found no `LB-102` heading, verified `/api/body-battery` takes no date, and filed work to build a capability that already existed. **Absence from a queue that removes completed entries is the signature of FINISHED.** Verifying the inputs is not verifying the conclusion: ask whether the capability exists ANYWHERE before filing work to build it.

## Blocked

- **Owner decisions:** BF-126, Q-551, the macro/budget anchor, LB-61's switch colour, PS-35's PWA landing.
- **BF-110 waits on the owner:** one blank resume in normal use, then read `bf110 resume recheck%` — **`stuck` → native, `resized` → render timing**. No fix before that row.
- **LB-106 is open with its fix UNCLAIMED.** Its stated cause was wrong: the log says `page.goto: net::ERR_ABORTED` at the **relaunch**, so the poll it blamed is never reached. **If it aborts again on `fresh.goto`, the relaunch shape was not it either** — drop the SW block and suspect the runner.
- **BF-100's own fix needs the device.** Do NOT re-run the synthetic-`touchstart` probe: driven 2026-09-15, both arms restored 840, which refutes the PROBE not the hypothesis (the element picked need not be the hook's `ref`; the dispatch may miss `RESTORE_WINDOW_MS`). Instrument both before reading anything into a re-run.
- **~50 VERIFY entries owe a look** (BF-136/LB-99 sharpest). **⚠ OR-108's picture will not show on the S25 until `LA-36` lands.** **⚠ BF-94 is parked on `Needs: BF-61`; BF-84 reads startable and is not; PS-4 is UNCLASSIFIED by design; LB-94 the owner deferred.** **⚠ Q-254 is device-free Lane B work parked behind `Needs: Q-297`**, itself `Gate: owner`; its premise is stale (says one spec exists, there are **84**). Do not unpark.

## Claimed paths

None held. #265 and #608 are abandoned Lane B PRs whose work is on `main`; closing one needs the owner.

## Do not re-litigate

- **`packages/shared/**`, `app/api/**`, `lib/data/**`, `lib/sqlite/**`, `lib/local-store/**`, `lib/cache-groups.ts`, `lib/coach/**` are Lane A** — the **path**, not the nature of the edit. `scripts/**` is the Orchestrator's, bar a shrink-only baseline the check demands. **Clearing ANOTHER role's completed entry is the Orchestrator's sweep — file it, do not do it.** Your own, from your own PR, you fix.
- **A Lane B half needing a Lane A argument is TWO entries.** `workout-screen.tsx` is shrink-only at 1835. **`saved-meals-sheet.tsx` is 791 against a HARD 800** — a new feature there goes in an extracted child.
- **E2E is ADVISORY** — but wait on it for app-code or new-spec PRs. **Batons are shrink-only**; cut narrative before adding. **⛔ IN ANY LINE OF AN ENTRY PARKS IT:** `next-item.js` reads it as the legacy prose blocker unless a structured `Gate:`/`Needs:` overrides, so a `⛔` used for EMPHASIS — which this repo does constantly — silently hides the entry. LB-116 sat in PARKED as an *"unmigrated marker"* for exactly that.

## The lessons that cost real time

1. **RUN THE CONTROL. Every time. It is two minutes and it is the only thing that separates a fix from a green test.** LA-109 settled two entries in one run: its own test went red without the fix, and its BF-49 cross-check **passed** without it, so that test was vacuous and the entries are not linked. **LB-116's e2e was DELETED rather than shipped:** it passed against unfixed `main`, because `saveMoodLog` derives the list when none is sent — **when a server fallback produces the same observable as the fix, no end-to-end assertion can separate them.** **#1284 is the shape to copy:** the loosened assertion was re-run with the restore neutered (`el.scrollTop = 0`) and both cases went red naming the cancellation — without that second run it is an assertion loosened until it stopped complaining. **⚠ AND YOUR CHANGE CAN MAKE ANOTHER'S TEST VACUOUS WITHOUT MAKING IT RED:** BF-168's effects added an earlier `setConfirmLeaveOpen`, so BF-166's `indexOf` ordering check went on passing while measuring my effect. **Re-read any assertion anchored on a string you just added.**
2. **⛔ CHECK YOUR OWN ENTRIES HARDEST — three of today's four corrections were mine.** **LB-114**: I filed it off ONE 07:55 observation, read the clause backwards from that single point, and wrote *"do not verify outside that window"* — sending the next session to verify a fix in the one window that cannot show the other half. It is red for TWO hours, the second (00:00–01:00) from a different line in a different file: before 07:00 the default wake time is in the FUTURE, so the future-wake guard falls back to local midnight. Confirmed red 00:41/00:46/00:50, green 01:06. **BF-165's retraction**: half wrong. **LB-116**: shipped and left in the queue, the same omission I had corrected for BF-172 hours earlier. **Citing your own entry is not checking it**; the tell is a result that falls outside what you wrote.
3. **An entry's numbers, gate, CAUSE and RECOMMENDED FIX are prose until something checks them — FIFTEEN have been wrong.** BF-166 said *"no overlay registry exists"* and one did (`sheet-back-stack.ts`) — building what it asked would have ADDED a defect. BF-168's proposed `pathname` dismissal would not fire for its own case. BF-167's proposed replacement turns BF-8's guard red. OR-118 said *"every number it renders already exists"*; the grouping did and the numbers did not. LB-113 said *"two call sites"* and one was inside `syncHealthConnect`. BF-175's sweep was the right grep and the WRONG AXIS — a key-grep cannot see a budget passed as a **prop**, and the second surface was one. **Read the thing the fix would touch before implementing it.**
4. **⛔ `tapCentre` DOES NOT SCROLL, and `touchscreen.tap` has NO actionability check — a control below the fold is tapped outside the viewport and NOTHING happens, silently.** On `/cardio` at 412×915 the three modality controls sit at y=852/924/997, so only Run was ever tapped — manufacturing *"both failures share the `/activity` prefix"*, which reads as a routing defect and is a coordinate one. **`elementFromPoint` returns null off-screen: assert it hit-tests to your control BEFORE dispatching**, after `scrollIntoView({ block: 'center' })`. `tapInView` does NOT cover this — it filters on **x** only. **And warm the destination first:** `next dev` compiles on demand and a client push hangs until it finishes, indistinguishable from a dead tap.
5. **WRITE DOWN WHAT WOULD DISPROVE YOUR FIX.** A 2026-08-30 note — *"if the abort returns, the SW was not it"* — settled LB-106 a fortnight later with **no new run**.
6. **`total_count: 0` HAS TWO CAUSES:** a stale base, and **`mergeable_state: dirty`** (no run is created at all). **The reliable green check is attempting the merge** — it validates against real branch protection and refuses a genuinely pending check.
7. **A PROBE MUST CREATE THE STATE IT NEEDS**, and a CI-only e2e failure can live in the **Postgres service-container log**. Never run `pnpm build` and vitest against the one local Postgres at once — a "failure" that will not reproduce serially is contention.

## Gotchas worth carrying

- **RUN `npx tsc --noEmit -p tsconfig.tests.json` BEFORE EVERY PUSH — plain `npx tsc --noEmit` TYPECHECKS NOTHING UNDER `__tests__`.** `node scripts/check-test-typecheck.js` runs it with the baseline. `e2e/` **is** covered. **Knowing the gotcha is not running the command** — BF-5 pushed red on it after this baton already said so.
- **RUN `pnpm vitest run` WHOLE before pushing**, not just the files you touched: this repo has tests aimed at your prose and at regions of your file.
- **A leftover `next-server` holds port 3100 without serving it** and `ss -ltn` shows nothing. Check `curl -s -o /dev/null -w '%{http_code}' localhost:3100` and `ps -o etime`. **⛔ `pkill -f "next dev"` KILLS YOUR OWN SHELL** — the pattern matches the wrapping command line (exit 144, no log). Never use it.
- **The doc-size baseline is `wc -l` + 1 exactly and the check FAILS ON SLACK** — recompute, never splice. `doc-size-baseline-history.md` is APPEND-ONLY. On the backlog a conflict is usually two *deletions* — keep neither, unless both sides EDITED different entries.
- **`refusing to merge unrelated histories` is the shallow-clone graft** — `git fetch origin --deepen=200`. **Rebuild `package.json` / `changelog.ts` / `projectOverview.md` from `git show origin/main:…`, never splice.** **After ANY merge run `git status --short | grep '^UU'`.**
- **`usePathname()` reads Next's route tree**, so it is useless for any bug whose symptom IS a stale tree — `window.location` is the only honest reader. **`page.goto()` to a sub-route rebuilds history**, so a spec about back must reach the screen by tapping.
- **`locator.click()` does nothing on Nutrition and times out on `/health`** (63 retries) — use `.evaluate(el => el.click())` with a `waitForFunction` guard. **`getByText` resolves to the INNERMOST match.**
- **The Morning Check-in is a MODAL and Radix `aria-hidden`s `<main>` behind it** — every role query returns nothing and it reads as "element not found". Call `suppressMorningCheckin(page)` **before** `page.goto`.
- **`seed.sql` records NOTHING for today** — a spec asserting a day's content is red here, green on CI. **Judge a colour by sampling pixels**, a layout by `getBoundingClientRect()`.
- **The admin screens need `is_admin` AND a re-minted JWT.** Re-run `--project=setup`; restore both. **Prove a destructive path from the DATABASE, not `page.route`.**
