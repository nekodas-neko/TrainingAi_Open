# 🚧 Implementation Agent (B) — baton

> **Successor sessions are titled `🚧 Implementation Agent (B) 🟢`** — exactly. A renamed successor is a lost thread.

**Updated:** 2026-09-21 · **By:** the thirty-sixth Lane B run · **Next ID:** `LB-122`

> **A mistyped ID here silently advances the lane's numbering.** Allocate with `grep -rhoE '\bLB-[0-9]+\b' docs/ | sort -t- -k2 -n | tail -1`, and check the max in the **journal** too — a shipped entry leaves the queue. The pointer above is a floor, not an allocation.

## Now

**v1.457.11→v1.462.1. Twenty-one PRs.** Recent: **#1350** BF-186 · **#1355** TN-53's render half — `spanGaps: true` made ELEVEN charts draw a smooth line across days with no reading · **#1361** TN-3b's HR-chart overlay · **#1362** the TN-35 finding · **#1365** TN-35's overlay half · **RV-81** one shared exercise datalist (3,900 `<option>` → 145). **LB-120 IS NOW THE DOMINANT COST: #1365 took SIX merge attempts and FIVE drifts** (#1363, #1364, #1341, #1367, #1366). Each push restarts a ~7-min check cycle and `main` was landing a PR every ~8 min, so the window never opened; the conflicts were always the same two files — `doc-size-baseline-history.md` and the `.size` baselines — which every agent touches on every PR. `enable_pr_auto_merge` refuses with *"already clean"* and is NOT a way out. **Attempting the merge is the only reliable green check.** **LB-121** (`⛔` is both the parser's blocker and the house emphasis glyph). **`check:rules` is `Ran 75 of 75`** — quote the count, not "pass".

**READY WAS 0 AND WRONG FOUR TIMES, THEN A REVIEW SWEEP REFILLED IT.** ① OR-118: a `⛔` used for EMPHASIS parked it (LB-121). ② TN-53: **no `Lane:` field**, so it printed UNCLASSIFIED. ③ TN-3b: I recommended STRIKING it as leftover prose — **the owner said no** and it became v1.461.0. **I had treated his reshape as SUPERSEDING the original ask when it only ADDED to it**; an expanding decision reads identically to a replacing one. ④ TN-35: `Needs:` satisfied but still blocking, because it clears only when the target LEAVES the queue and TN-3b stays for a `Keep:`. **Filed as #1362, NOT self-unparked**; the owner said to continue, which authorised the build. **That question is the Orchestrator's — TN-35 shipping is not its answer.** **#1341 then filed RV-64…RV-83 and READY is 4.** Next: the `motion-polish` batch (RV-71 the shared Button has zero `active:` on a touch-only product while 45 files hand-roll `active:scale`; RV-75 sheets open in 500 ms against the app's tuned 180–200 ms), then RV-79 (Home reads mood with a bare `fetch`). **BF-185 is Lane A's:** *"the server already honours an explicit `takenAt`"* is true of the repository and FALSE of the route — `SupplementLogSchema` is `.strict()` with three fields. **Read the SCHEMA, not the repository.** **Owed to the owner, all device:** `back-gesture-sitting` (BF-100, BF-166, LB-107, LA-109) is ONE sitting · TN-3b's crowded HR chart at 412 px · TN-35's event list · TN-53's stranded dot · OR-118 · BF-186's tap target · BF-177 · TN-50 · BF-175 · BF-165 · TN-25.

**A 4-hourly silent Routine polls this lane** (`trig_01WcuYTidPtngLFZFD7yKnoL`): syncs `main`, clears any open PR, runs `next-item.js`, **says nothing when READY is 0**.

## Next

1. **RE-SCAN BEFORE CONCLUDING ANYTHING — this has fired SEVEN times.** READY went 6→9 on Lane A's BF-155; BF-161 became startable minutes after I called the lane empty; **TN-25 was re-laned A→B by Lane A and became the day's build.** **A "blocked" or "empty" finding has a shelf life of one merge.**
2. **BF-165: ROOT CAUSE FOUND (#1281), FIX STILL OPEN.** `closeSurface` pops the entry the sheet pushed, and `selectType` runs that close immediately before `router.push` — so the pop lands on the navigation. **⛔ The obvious fix was BUILT and DOES NOT WORK:** waiting for `pendingSelfPops` to drain cannot help, because the close is **415 ms late** — it runs in an effect cleanup needing a React commit, and `startViewTransition` holds that commit. A real fix must stop the entry being popped once a navigation supersedes it, keyed on **surface identity** (a module flag is the BF-34 known-bad).
3. **ASKING BEAT BUILDING on TN-13.** Its fix was one line; put to the owner as four drawn options, the answer was **leave it**. **Two owner questions remain open**, shipped around rather than blocked on: RV-38's `—`-versus-badge posture, and OR-116's three-surfaces-one-number. **But a decision an entry DELEGATES to this lane is yours: make it, and write the reasoning into the code** (TN-25's pre-set-vs-suggest).
4. **READY running low is not "no work" — READ THE KEEPS AND THE ENTRIES WHOLE.** LA-108 printed under KEEP as "not new work" while its residue WAS the work; BF-5's `Keep:` was a whole feature; BF-100's *second* finding shipped as #1284 from inside a device-blocked entry. **When READY is thin, read every residue for the word that means BUILD.** Also read PARKED (the console truncates it); TN-3b sat there three days while READY was 0. **And a red CI job on your own PR is work: #1302 was the day's second ship and came entirely out of one failing check.**
5. **⛔ DO NOT REPEAT MY LB-110 MISTAKE (removed in #1238, corrected in #1239):** I verified `/api/body-battery` takes no date and filed work to build a capability that already existed. **Absence from a queue that removes completed entries is the signature of FINISHED.** Ask whether the capability exists ANYWHERE before filing work to build it.

## Blocked

- **Owner decisions:** BF-126, Q-551, the macro/budget anchor, LB-61's switch colour, PS-35's PWA landing.
- **BF-110 waits on the owner:** one blank resume in normal use, then read `bf110 resume recheck%` — **`stuck` → native, `resized` → render timing**.
- **LB-106 is open with its fix UNCLAIMED.** Its stated cause was wrong: the log says `page.goto: net::ERR_ABORTED` at the **relaunch**, so the poll it blamed is never reached. **If it aborts again on `fresh.goto`, the relaunch shape was not it either** — suspect the runner.
- **BF-100 is SETTLED in the harness and open only on the device (#1324).** The `touchstart` cancel is measured, not suspected: listeners attach **182 ms before** the restore lands, and with an unreachable target seeded the pre-fix hook reports `restored to 0 against a reachable 1019`. **Do not re-run the synthetic probe in any form** — `page.goBack()` does not resolve until after the mount and restore, so the window is unreachable from the test side AT ALL; that is why the 2026-09-15 null meant nothing.
- **~50 VERIFY entries owe a look** (BF-136/LB-99 sharpest). **⚠ OR-108's picture will not show on the S25 until `LA-36` lands.** **⚠ BF-94 is parked on `Needs: BF-61`; BF-84 reads startable and is not; PS-4 is UNCLASSIFIED by design; LB-94 the owner deferred.** **⚠ Q-254 is device-free Lane B work parked behind `Needs: Q-297`**, itself `Gate: owner`; its premise is stale (says one spec exists, there are **84**). Do not unpark.

## Claimed paths

None held. #265 and #608 are abandoned Lane B PRs whose work is on `main`; closing one needs the owner.

## Do not re-litigate

- **`packages/shared/**`, `app/api/**`, `lib/data/**`, `lib/sqlite/**`, `lib/local-store/**`, `lib/cache-groups.ts`, `lib/coach/**` are Lane A** — the **path**, not the nature of the edit. `scripts/**` is the Orchestrator's. **Clearing ANOTHER role's completed entry is the Orchestrator's sweep — file it, do not do it.** `lib/walk/**` and `lib/hooks/**` are ours (no `app/api` route reaches the new files).
- **A Lane B half needing a Lane A argument is TWO entries.** `workout-screen.tsx` is shrink-only at 1835. **`saved-meals-sheet.tsx` is 791 against a HARD 800** — a new feature there goes in an extracted child.
- **E2E is ADVISORY** — but wait on it for app-code or new-spec PRs. **Batons are shrink-only**; cut narrative before adding. **⛔ IN ANY LINE OF AN ENTRY PARKS IT:** `next-item.js` reads it as the legacy prose blocker unless a structured `Gate:`/`Needs:` overrides, so a `⛔` used for EMPHASIS — which this repo does constantly — silently hides the entry.

## The lessons that cost real time

1. **RUN THE CONTROL. Every time. It is two minutes and it is the only thing that separates a fix from a green test — and the only thing that separates "not my failure" from a guess.** #1299's E2E went red in a spec asserting on the very component it edits; a control run of that spec on clean `origin/main` reproduced it exactly, which is what made "not mine" a fact. The same move settled LA-109 (its own test red without the fix, its BF-49 cross-check green without it, so that one was vacuous). **LB-116's e2e was DELETED rather than shipped:** it passed against unfixed `main`, because `saveMoodLog` derives the list when none is sent — **when a server fallback produces the same observable as the fix, no end-to-end assertion can separate them.** **Mutate the fix and watch the test go red, and read WHERE it goes red** — TN-25's two mutations failed at two different assertions, and only the second exercised the one that mattered. **⛔ TN-50 is the sharpest case: mutating the `useState` initialiser left the e2e GREEN**, because the reset effect fires on open and overwrites it — the load-bearing line was the reset arm. A spec that cannot see the bug looks identical to one that passes. **BF-177 is what a good control prints:** with the fix removed it reported `kcal left went 1810 → 1810`, the owner's report reproduced in one line. **⚠ AND YOUR CHANGE CAN MAKE ANOTHER'S TEST VACUOUS WITHOUT MAKING IT RED:** BF-168's effects added an earlier `setConfirmLeaveOpen`, so BF-166's `indexOf` check went on passing while measuring my effect.
2. **⛔ READ THE FAILURE'S PAGE SNAPSHOT BEFORE THE SCARIEST LINE IN THE LOG.** The `la109` red shipped with a React hydration mismatch on Home in the same job — server `Tuesday 10 March`, client `Friday 18 September` — which reads as a live SSR bug and is **`page.clock.install` in `day-rollover-checkin.spec.ts`**, a deliberately faked client clock. Filing that would have been a phantom entry against Home. The real cause was in `error-context.md`: Home rendered perfectly under `heading "Good night, …"`, and the spec waited for `/Good (morning|afternoon|evening)/` while `getGreeting` has **four** periods. **A test that reads the real clock is red for whichever hours it forgot** — and the fix is to match by SHAPE, because listing the periods rebuilds the trap on the fifth.
3. **⛔ CHECK YOUR OWN ENTRIES HARDEST — three of one day's four corrections were mine.** **LB-114**: filed off ONE 07:55 observation, clause read backwards, and I wrote *"do not verify outside that window"* — sending the next session to the one window that cannot show the other half. It is red for TWO hours. **BF-165's retraction**: half wrong. **LB-116**: shipped and left in the queue, the same omission I had corrected hours earlier. **Citing your own entry is not checking it**; the tell is a result that falls outside what you wrote.
4. **An entry's numbers, gate, CAUSE and RECOMMENDED FIX are prose until something checks them — SIXTEEN have been wrong.** BF-166 said *"no overlay registry exists"* and one did. OR-118 said *"every number it renders already exists"*; the grouping did and the numbers did not. BF-175's sweep was the right grep and the WRONG AXIS — a key-grep cannot see a budget passed as a **prop**. **TN-25 said the wiring was "the WHOLE remaining ask" and the selector could not reach a config at all**: no set count, and two patterns byte-identical in its table. **Read the thing the fix would touch before implementing it.**
5. **⛔ `tapCentre` DOES NOT SCROLL, and `touchscreen.tap` has NO actionability check — a control below the fold is tapped outside the viewport and NOTHING happens, silently.** On `/cardio` at 412×915 the modality controls sit at y=852/924/997, so only Run was ever tapped — manufacturing *"both failures share the `/activity` prefix"*, which reads as a routing defect and is a coordinate one. **`elementFromPoint` returns null off-screen: assert it hit-tests to your control BEFORE dispatching**, after `scrollIntoView({ block: 'center' })`. `tapInView` filters on **x** only. **And warm the destination first.**
6. **WRITE DOWN WHAT WOULD DISPROVE YOUR CLAIM, THEN GO AND RUN IT.** A 2026-08-30 note — *"if the abort returns, the SW was not it"* — settled LB-106 a fortnight later with **no new run**. And **LB-114's two-hour window is now CONFIRMED, not inferred**: `rv38` failed every run inside 00:00–01:00 Brisbane and PASSED the full suite on a run started at 01:15, which is what retro-justifies merging #1299 and #1303 while their own E2E was red on it. A window claim is cheap to test — wait for the window to close. **⛔ AND THE MIRROR, WHICH COST BF-100 FIVE DAYS: A NULL RESULT IS ONLY EVIDENCE IF YOUR PROBE COULD HAVE SEEN THE EFFECT.** Its 2026-09-15 probe dispatched a touch and nothing cancelled, and the entry recorded that as inconclusive-but-suspicious for two stated reasons, both wrong. The real one: `page.goBack()` does not resolve until after the mount AND the restore, so the dispatch was on the wrong side of a 182 ms window by construction — unhittable from the test side at all. **Before trusting a null, state what would make the instrument blind, then check THAT.** And when a window is too narrow to hit, do not chase it: **widen it** — seeding an unreachable scroll target turned 182 ms into the full 15 s and the bug reproduced on the first try.
7. **⛔ SHIPPING THE CODE AND CLEARING THE QUEUE ARE TWO ACTS, AND THE SECOND IS THE ONE THAT GETS DROPPED — three times in one session** (LB-116, BF-172, RV-57), twice AFTER correcting it. The PR feels finished when CI goes green. **The only thing that catches it is running `next-item.js` AFTER the merge, not before** — RV-57 printed as the top of a lane I was about to report as empty. Do that re-scan before any "the lane is blocked" claim.

## Gotchas worth carrying

- **RUN `npx tsc --noEmit -p tsconfig.tests.json` BEFORE EVERY PUSH — plain `npx tsc --noEmit` TYPECHECKS NOTHING UNDER `__tests__`.** `node scripts/check-test-typecheck.js` runs it with the baseline; `e2e/` **is** covered. Then `pnpm vitest run` WHOLE (this repo has tests aimed at your prose) and `pnpm check:rules`, the only thing that counts as custom-rules-pass. **Knowing the gotcha is not running the command.**
- **⛔ DO NOT COPY A SEED-THEN-FETCH `useEffect(…, [])` FROM AN EXISTING SCREEN.** 36 such sites are FROZEN by `check-fetch-once-effects.js`; copying one fails the gate. Use `useCachedValue(key, url, ttl, { today })` — it takes the today-variant flag, and re-fetches on invalidation, which the hand-rolled shape never does.
- **A leftover `next-server` holds port 3100 without serving it** and `ss -ltn` shows nothing. **⛔ `pkill -f "next dev"` KILLS YOUR OWN SHELL** (exit 144, no log). Never use it.
- **The doc-size baseline is `wc -l` + 1 exactly and the check FAILS ON SLACK** — recompute, never splice; the script is `check-doc-index-size.js`. `doc-size-baseline-history.md` is APPEND-ONLY. On the backlog a conflict is usually two *deletions* — keep neither, unless both sides EDITED different entries.
- **`total_count: 0` is NEVER slow CI** — it is a stale base or `mergeable_state: dirty`, and **while dirty a push creates NO workflow run at all**, so the PR sits looking like it is building; confirm with `list_workflow_runs` on the branch and compare `head_sha`. Attempting the merge is the reliable green check; `expectedHeadSha` wants the **full 40 chars**. **`refusing to merge unrelated histories` is the shallow-clone graft** — `git fetch origin --deepen=100`. **Rebuild `package.json` / `changelog.ts` / `projectOverview.md` from `git show origin/main:…`, never splice.** **After ANY merge run `git status --short | grep '^UU'`.** Expect to re-merge `main` two or three times per PR on a busy day.
- **`usePathname()` reads Next's route tree**, so it is useless for any bug whose symptom IS a stale tree — `window.location` is the only honest reader. **`page.goto()` to a sub-route rebuilds history**, so a spec about back must reach the screen by tapping.
- **`locator.click()` does nothing on Nutrition and times out on `/health`** (63 retries) — use `.evaluate(el => el.click())` with a `waitForFunction` guard. **`getByText` resolves to the INNERMOST match.**
- **The Morning Check-in is a MODAL and Radix `aria-hidden`s `<main>` behind it** — every role query returns nothing and it reads as "element not found". Call `suppressMorningCheckin(page)` **before** `page.goto`.
- **`seed.sql` records NOTHING for today** — a spec asserting a day's content is red here, green on CI. **Judge a colour by sampling pixels**, a layout by `getBoundingClientRect()`. **The chromium SIGSEGV (LB-119) takes unrelated specs down with `browser.newContext: … has been closed` — that spec never ran.**
- **The admin screens need `is_admin` AND a re-minted JWT.** Re-run `--project=setup`. **Prove a destructive path from the DATABASE, not `page.route`.**
