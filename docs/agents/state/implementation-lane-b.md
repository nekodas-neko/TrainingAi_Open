# 🚧 Implementation Agent (B) — baton

> **Successor sessions are titled `🚧 Implementation Agent (B) 🟢`** — exactly. A renamed successor is a lost thread.

**Updated:** 2026-09-22 · **By:** the thirty-sixth Lane B run · **Next ID:** `LB-125`

> **A mistyped ID here silently advances the lane's numbering.** Allocate with `grep -rhoE '\bLB-[0-9]+\b' docs/ | sort -t- -k2 -n | tail -1`, and check the max in the **journal** too — a shipped entry leaves the queue. The pointer above is a floor, not an allocation.

## Now

**v1.457.11→v1.464.1. Thirty-three PRs.** Recent: **#1384** LB-124 + TN-58's gate · **#1386** RV-64 · **#1387** a RED `main` (see below) · **RV-84+RV-88**. **Review sweep 52 (#1368) landed 2026-09-22 and took READY from 0 to 15** — the lane is not short of work now. **⛔ `git fetch origin main` CAN LEAVE `origin/main` STALE — measured 2026-09-21.** It read **0 behind** on a branch GitHub called `dirty`. **Always `git fetch origin main --force --prune`.** **LB-120 is still the dominant cost** and the conflict is ALWAYS `doc-size-baseline-history.md` + the `.size` baselines — **proposal filed: split the history file per-PR**, as LA-33 did for `.size`. `enable_pr_auto_merge` refuses with *"already clean"*; **attempting the merge is the only reliable green check.** **`check:rules` is `Ran 75 of 75`.**

**A SHIPPED ENTRY WITH NOTHING OWED MUST BE DELETED, NOT ANNOTATED** — RV-81 was annotated and printed as the top of READY next scan; `check-backlog-pointers.js` missed it because the ✅ was in a bullet. **LB-121 (a `⛔` used for EMPHASIS parks an entry) had FOUR instances; RV-72's is now removed.** **READY 0 IS NOT EMPTY — RV-64 CAME OUT OF THE KEEPS**, its residue the whole remaining ask with no gate, printing as "not new work". **Read every residue before reporting the lane blocked.** **⛔ AND CHECK `main` IS GREEN BEFORE BLAMING YOUR OWN PR:** #1386's `Tests` red was `cardio-hub-routes` asserting `Math.round(spanDays) === 90` against a LOCAL-MIDNIGHT-anchored window — span 90.0–91.0, `Math.round` tips at local **12:00**, so `main` was red for ~12h of every 24 on every branch (#1387). Run the control on clean `origin/main` first. **SIX entry claims this run did not survive contact.** **RV-84 said 16 dead `.catch`es; there are 81** — 68 harmless no-ops, 4 redundant (including the file it names as its own reference), **9** real. **RV-88's second defect is not one in that file** — `computeHrZones` takes name and colour from fixed defs, so an invented profile changes nothing rendered. **And three were wrong about the field deciding who could build them:** RV-79's `cachedFetchCore` calls `setCached` UNCONDITIONALLY after any 2xx so `onData` cannot veto a null (proven — `onData` fired twice, `readCacheSync` then read `null`, the session-167 bug); BF-185's `SupplementLogSchema` is `.strict()`; **TN-58's comparative field exists NOWHERE and its named "engine half" TN-57 ships no migration** — and the route is not `.strict()`, so a control would post **201 and store nothing**, silently burning a two-week pass test. **Read the SCHEMA and the WRITERS, not the entry.** **Owed to the owner, all device:** `back-gesture-sitting` (BF-100, BF-166, LB-107, LA-109) is ONE sitting · `motion-polish` (RV-71, RV-72, RV-75, RV-74) is ANOTHER · TN-3b's crowded HR chart at 412 px · TN-35's event list · TN-53's stranded dot · OR-118 · BF-186 · BF-177 · TN-50 · BF-175 · BF-165 · TN-25.

**A 4-hourly silent Routine polls this lane** (`trig_01WcuYTidPtngLFZFD7yKnoL`): syncs `main`, clears any open PR, runs `next-item.js`, **says nothing when READY is 0**.

## Next

1. **RE-SCAN BEFORE CONCLUDING ANYTHING — this has fired SEVEN times.** READY went 6→9 on Lane A's BF-155; **TN-25 was re-laned A→B by Lane A and became the day's build.** **A "blocked" or "empty" finding has a shelf life of one merge.**
2. **BF-165: ROOT CAUSE FOUND (#1281), FIX STILL OPEN.** `closeSurface` pops the entry the sheet pushed, and `selectType` runs that close immediately before `router.push` — so the pop lands on the navigation. **⛔ The obvious fix was BUILT and DOES NOT WORK:** waiting for `pendingSelfPops` to drain cannot help, because the close is **415 ms late** — it runs in an effect cleanup needing a React commit, and `startViewTransition` holds that commit. A real fix must stop the entry being popped once a navigation supersedes it, keyed on **surface identity** (a module flag is the BF-34 known-bad).
3. **ASKING BEAT BUILDING on TN-13.** Its fix was one line; put to the owner as four drawn options, the answer was **leave it**. **Two owner questions remain open**: RV-38's `—`-versus-badge posture, and OR-116's three-surfaces-one-number. **But a decision an entry DELEGATES to this lane is yours: make it, and write the reasoning into the code** (TN-25's pre-set-vs-suggest).
4. **READY running low is not "no work" — READ THE KEEPS AND THE ENTRIES WHOLE.** LA-108 printed under KEEP as "not new work" while its residue WAS the work; BF-5's `Keep:` was a whole feature. **When READY is thin, read every residue for the word that means BUILD.** Also read PARKED (the console truncates it); TN-3b sat there three days while READY was 0. **And a red CI job on your own PR is work: #1302 came entirely out of one failing check.**
5. **⛔ DO NOT REPEAT MY LB-110 MISTAKE (removed in #1238):** I verified `/api/body-battery` takes no date and filed work to build a capability that already existed. **Absence from a queue that removes completed entries is the signature of FINISHED.**

## Blocked

- **Owner decisions:** BF-126, Q-551, the macro/budget anchor, LB-61's switch colour, PS-35's PWA landing.
- **BF-110 waits on the owner:** one blank resume in normal use, then read `bf110 resume recheck%` — **`stuck` → native, `resized` → render timing**.
- **LB-106 is open with its fix UNCLAIMED.** Its stated cause was wrong: the log says `page.goto: net::ERR_ABORTED` at the **relaunch**, so the poll it blamed is never reached. **If it aborts again on `fresh.goto`, the relaunch shape was not it either** — suspect the runner.
- **BF-100 is SETTLED in the harness and open only on the device (#1324).** **Do not re-run the synthetic probe in any form** — `page.goBack()` does not resolve until after the mount and restore, so the window is unreachable from the test side AT ALL.
- **~50 VERIFY entries owe a look** (BF-136/LB-99 sharpest). **⚠ OR-108's picture will not show on the S25 until `LA-36` lands.** **⚠ BF-94 is parked on `Needs: BF-61`; BF-84 reads startable and is not; PS-4 is UNCLASSIFIED by design; LB-94 the owner deferred.** **⚠ Q-254 is device-free Lane B work parked behind `Needs: Q-297`**, itself `Gate: owner`; its premise is stale. Do not unpark.

## Claimed paths

None held. #265 and #608 are abandoned Lane B PRs whose work is on `main`; closing one needs the owner.

## Do not re-litigate

- **`packages/shared/**`, `app/api/**`, `lib/data/**`, `lib/sqlite/**`, `lib/local-store/**`, `lib/cache-groups.ts`, `lib/coach/**` are Lane A** — the **path**, not the nature of the edit. `scripts/**` is the Orchestrator's. **Clearing ANOTHER role's completed entry is the Orchestrator's sweep — file it, do not do it.** `lib/walk/**` and `lib/hooks/**` are ours.
- **A Lane B half needing a Lane A argument is TWO entries** — RV-79/LB-123 is the clean example. `workout-screen.tsx` is shrink-only at 1835. **`saved-meals-sheet.tsx` is 791 against a HARD 800.**
- **E2E is ADVISORY — but wait on it ONCE for a PR carrying a NEW spec.** #1377 went red, the new spec **passed**, and the hard failure was LB-119's `SIGSEGV` at `0x1b0` taking `diary-nested-meal:231` — **the line LB-119's table already records from #1280**. Merging blind ships an unread red; blocking on the re-run loops forever against a ~31-min job while `main` lands a PR every ~8 min. **Read it once, then merge on the five REQUIRED checks.**
- **⛔ `freshWithinTtl: true` NEEDS EVERY WRITER IN A GROUP, not the key in two groups (RV-64).** The entry claimed the proof was "available"; `hr-profile` is computed over a 90-day `getHrForWindow` and live BLE samples land there **during the workout**, ingested server-side with nothing in `lib/live-hr/**` invalidating. It would have pinned six hours across workouts to fix a 20-call loop inside one. **Ask what writes the PAYLOAD.** **Batons are shrink-only**; cut narrative before adding.

## The lessons that cost real time

1. **RUN THE CONTROL. Every time.** It is the only thing separating a fix from a green test, and "not my failure" from a guess. **Mutate the fix and watch the test go red, and read WHAT it claims while it is there** — TN-25's two mutations failed at two different assertions and only the second mattered; RV-72's four each named the right defect, and the `width` mutation killed the LOCATOR, so the later assertions were never exercised until a narrower one was run. **⛔ TN-50 is the sharpest case: mutating the `useState` initialiser left the e2e GREEN** — the reset effect overwrote it, and the load-bearing line was the reset arm. **LB-116's e2e was DELETED rather than shipped:** it passed against unfixed `main` because the server derives the same observable.
2. **⛔ A GUARD ON AN ELEMENT THAT ONLY RENDERS IN A NON-DEFAULT STATE PASSES VACUOUSLY (RV-72, new).** The calorie bar's gradient fill is conditional on `intake > 0` and the seeded day is **empty**, so the exclusion guard matched nothing and read as green. It had to move into `calorie-progress-bar.spec.ts` — the only spec whose fixture seeds a real intake. **Before writing an assertion, ask what the seed actually renders**; `/health` has no contributor chart either, it lives on `/health/readiness`.
3. **⛔ READ THE FAILURE'S PAGE SNAPSHOT BEFORE THE SCARIEST LINE IN THE LOG.** The `la109` red shipped with a React hydration mismatch that reads as a live SSR bug and is `page.clock.install` in another spec. The real cause was in `error-context.md`. **A test that reads the real clock is red for whichever hours it forgot** — match by SHAPE.
4. **⛔ CHECK YOUR OWN ENTRIES HARDEST — three of one day's four corrections were mine** (LB-114, filed off ONE observation with the clause backwards). **Citing your own entry is not checking it**; the tell is a result outside what you wrote. **An entry's numbers, gate, CAUSE and RECOMMENDED FIX are prose until something checks them — TWENTY have been wrong.** BF-166 said *"no overlay registry exists"* and one did. **RV-79's and RV-64's stated fixes would each have shipped the bug the same entry warned about.** **LA-124 predicted the existing test would catch a module-scope hoist: it catches the broad mutation (4 red) and MISSES the narrow one (27 of 28 green).** **Read the thing the fix would touch before implementing it.**
6. **⛔ `tapCentre` DOES NOT SCROLL, and `touchscreen.tap` has NO actionability check** — a control below the fold is tapped outside the viewport and NOTHING happens, silently. **`elementFromPoint` returns null off-screen: assert it hit-tests to your control BEFORE dispatching**, after `scrollIntoView({ block: 'center' })`. `tapInView` filters on **x** only. **And SHIPPING THE CODE AND CLEARING THE QUEUE ARE TWO ACTS, the second dropped three times in one session — run `next-item.js` AFTER the merge, not before.**
7. **WRITE DOWN WHAT WOULD DISPROVE YOUR CLAIM, THEN GO AND RUN IT.** A 2026-08-30 note settled LB-106 a fortnight later with no new run. **⛔ AND THE MIRROR, WHICH COST BF-100 FIVE DAYS: A NULL RESULT IS ONLY EVIDENCE IF YOUR PROBE COULD HAVE SEEN THE EFFECT.** When a window is too narrow to hit, **widen it** — seeding an unreachable scroll target turned 182 ms into 15 s and it reproduced first try.

## Gotchas worth carrying

- **RUN `npx tsc --noEmit -p tsconfig.tests.json` BEFORE EVERY PUSH — plain `npx tsc --noEmit` TYPECHECKS NOTHING UNDER `__tests__`.** `node scripts/check-test-typecheck.js` runs it with the baseline; `e2e/` **is** covered. Then `pnpm vitest run` WHOLE and `pnpm check:rules`. **Knowing the gotcha is not running the command.**
- **⛔ DO NOT COPY A SEED-THEN-FETCH `useEffect(…, [])`.** 36 sites are FROZEN by `check-fetch-once-effects.js`. Use `useCachedValue(key, url, ttl, { today })`.
- **A leftover `next-server` holds port 3100 without serving it** and `ss -ltn` shows nothing. **⛔ `pkill -f "next dev"` KILLS YOUR OWN SHELL** (exit 144, no log). Never use it.
- **The doc-size baseline is `wc -l` + 1 exactly and the check FAILS ON SLACK** — recompute with `check-doc-index-size.js --fix`, never splice; **the placeholder must be `1`, not `0`**. `doc-size-baseline-history.md` is APPEND-ONLY, so keep BOTH sides. On the backlog a conflict is usually two *deletions* — keep neither; **grep the ids afterwards rather than reading the diff.**
- **`total_count: 0` is NEVER slow CI** — it is a stale base or `mergeable_state: dirty`, and **while dirty a push creates NO workflow run at all**. **To tell a slow job from a stale read use `list_workflow_jobs` and read the STEP's `started_at`/`completed_at`** — a 404 on the logs looks identical either way. `expectedHeadSha` wants the **full 40 chars**. **`refusing to merge unrelated histories` is the shallow-clone graft** — `git fetch origin --deepen=200`. **Rebuild `package.json` / `changelog.ts` from `git show origin/main:…`, never splice.** **After ANY merge run `git status --short | grep '^UU'`.**
- **`usePathname()` reads Next's route tree**, so it is useless for any bug whose symptom IS a stale tree. **`page.goto()` to a sub-route rebuilds history**, so a spec about back must reach the screen by tapping.
- **`locator.click()` does nothing on Nutrition and times out on `/health`** — use `.evaluate(el => el.click())` with a `waitForFunction` guard. **`getByText` resolves to the INNERMOST match.**
- **The Morning Check-in is a MODAL and Radix `aria-hidden`s `<main>` behind it.** Call `suppressMorningCheckin(page)` **before** `page.goto`.
- **`seed.sql` records NOTHING for today** — a spec asserting a day's content is red here, green on CI. **Judge a colour by sampling pixels**, a layout by `getBoundingClientRect()`, and a CSS change by **computed style, never class strings** (`duration-250` is not a Tailwind class and compiled to nothing).
- **The admin screens need `is_admin` AND a re-minted JWT.** Re-run `--project=setup`. **Prove a destructive path from the DATABASE, not `page.route`.**
