# 2026-09-03 — Q-252 asks for something the codebase decided against (Lane A)

**Branch:** `docs/q252-superseded` · docs-only.

Q-252 (*"error tracking with session replay"*) came up in Lane A's queue. Re-verifying it against
current `main` before implementing — the standing rule — found it mostly built and its headline ask
**decided against**.

| the entry asks for | status |
|---|---|
| error tracking beyond `error_events` | ✅ shipped (Q-404): `@sentry/nextjs`, three configs, `sentry-scrub.ts` |
| **session replay** | ⛔ **decided against, in code, in three files** |
| source-mapped stack traces | 🚧 in flight — BF-92 / #841 |
| release tagging | ❌ **still owed — the real residue** |
| breadcrumbs | ✅ SDK default |
| the DSN-in-public-repo question | ✅ answered in `instrumentation-client.ts` |

`instrumentation-client.ts`, `sentry.server.config.ts` and `sentry.edge.config.ts` all carry the same
line: *"No session replay and no profiling: both capture far more of a health screen than an alerting
tool needs, and the reason this vendor was accepted at all was alerting."* A deliberate privacy
decision, written in three places, taken **after** this entry was filed.

**Building it as titled would have reversed a made decision without anyone noticing it had been
made.** That is precisely the hazard the re-verify-before-implementing rule exists for, and it is the
first time this session that rule stopped work rather than merely refined it.

The entry is **not deleted**: release tagging is real and unbuilt. And replay is **not re-opened** —
if the owner wants it, that is a fresh decision with the privacy trade named, not a queue item
carried forward from before the decision existed.

## The premise underneath is also the wrong problem now

Q-252 argues that `error_events` cannot say what the user did before a throw. BF-92 measured
something more basic: **Sentry has received nothing from the browser at all** since setup, because
`connect-src` never named the ingest host. Richer client telemetry is not the next problem while
client telemetry is zero. Recommended: once BF-92 lands and a browser event is confirmed on device,
retitle this to *"release tagging"* or fold it into BF-92 and strike it.

## Nothing was built

No code changed. This is queue hygiene: an entry that would have produced work nobody wanted.
